#include "main.h"
#include <stdio.h>

/**
 *print_rev - function that prints a string, in reverse.
 *@s: a char.
 * Return: Always 0.
 */
void rev_string(char *s)
{
	 int i = str.length();

	 for(int i = 0 ; i < 2 ; i++ )
	  
}
