#include <stdio.h>
/**
 * main - put alphabet in lowecase.
 *
 * Return : Always 0.
 */
int main() {
    char c;
    for (c = 'a'; c <= 'z'; ++c){
        putchar(c);
    }
        putchar('\n');
 	return(0);       
}

