#include"main.h"

/**
 * int mul - function that multiplies two integers.
 *@a: integer a.
 *@b: integer b.
 * Return: multiplication.
 */
int mul(int a, int b)
{
	int mul;

	mul = (a * b);

	return(mul);
